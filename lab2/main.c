/* Студент: Ф. И. О. Группа: номер группы Назначение: вычисление площади и периметра прямоугольника */ 
#include <stdio.h> 
    int main(void) { 
        double a, b; 
        double area, perimeter; 
        printf("Введите длину и ширину: "); 
        scanf("%lf %lf", &a, &b); 
        area = a * b; 
        perimeter = 2.0 * (a + b);
        printf("========================================\n");
        printf(" ПАРАМЕТРЫ ПРЯМОУГОЛЬНИКА\n"); printf("========================================\n");
        printf("Длина: %.2f\n", a);
        printf("Ширина: %.2f\n", b);
        printf("Площадь: %.2f\n", area);
        printf("Периметр: %.2f\n", perimeter); printf("========================================\n");
        printf("Расчёт завершён успешно\n"); return 0; 
        }